import { Spinner } from "./ui/spinner";

const MySpinner = () => {
  return (
    <div className="flex-1 bg-linear-t-r from-violet-500 to-cyan-500">
      <Spinner />
    </div>
  );
};

export default MySpinner;
